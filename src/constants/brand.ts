/**
 * Brand constants — the single source of truth for MyStokk's core brand colors.
 * Import from here; never hardcode these hex values.
 */
export const BRAND = {
  navy: '#0F172A', // logo blocks, dark UI text
  wordmark: '#0B1220', // "MyStokk" text (near-black)
  primary: '#2563EB', // accent block, the "o", primary buttons
  mutedText: '#8A98AD',
} as const;
